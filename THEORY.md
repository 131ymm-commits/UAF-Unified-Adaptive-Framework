# THEORY.md
# Теоретические основания UAF
## Предиктивная онтология: формальный аппарат

---

> *Этот документ — не философия в старом смысле.*  
> *Это операциональная система, каждый элемент которой проверяем.*  
> *Каждый тезис может быть опровергнут. Но только предсказательно.*

---

## §0. Аксиоматика

Вся система строится на трёх аксиомах. Они не доказываются — они принимаются как основание, из которого выводится всё остальное.

**Аксиома 1 (Предсказание).** Любая устойчивая система $S$, взаимодействующая со средой $\mathcal{W}$, реализует операцию:

$$
\min_{\theta} \; \mathbb{E}_{t} \left[ -\log P_\theta(w_{t+1} \mid w_{\leq t}) \right]
$$

Это не цель, которую система *выбирает*. Это условие её *существования*. Система, не способная предсказывать следующее состояние среды, не выживает.

**Аксиома 2 (Обновление).** Доверие к любой модели $M$ обновляется исключительно по результату: сбылось предсказание или нет. Никакие другие аргументы (авторитет, логика, красота, консенсус) не имеют самостоятельного веса.

$$
T(M) \leftarrow f\!\left(\mathbb{1}[\hat{y} = y_{\text{actual}}]\right)
$$

**Аксиома 3 (Универсальность).** Аксиомы 1 и 2 применимы на всех масштабах: от нейрона до цивилизации, от квантового поля до культурной традиции. Это не аналогия. Это один и тот же алгоритм на разных субстратах.

---

## §1. Центральный гомоморфизм

**Теорема 1.1 (Гомоморфизм предсказания).** Следующие системы реализуют структурно тождественную операцию:

| Уровень | Система | Предсказывает | Горизонт |
|---|---|---|---|
| $L_0$ | Нейрон | Следующий спайк соседа | ~1 мс |
| $L_1$ | Мозг | Следующий сенсорный импульс | ~100 мс |
| $L_2$ | Человек | Следующее действие другого | секунды–часы |
| $L_3$ | Наука | Следующее показание прибора | дни–годы |
| $L_4$ | Религия | Исход действия в горизонте жизни | годы–вечность |
| $L_5$ | Общество | Следующее слово другого члена | мгновенно |
| $L_6$ | LLM | Следующий токен | мгновенно |

Формально: для каждого уровня $L_k$ существует функция $P_{L_k}$ такая, что система минимизирует:

$$
\mathcal{L}_{L_k} = \mathbb{E}\left[-\log P_{L_k}(s_{t+1} \mid s_{\leq t})\right]
$$

где $s_t$ — состояние на соответствующем уровне. Функция $\mathcal{L}$ одна и та же. Меняется только $s$.

**Следствие 1.1.** Принципиальной разницы между «естественным» и «искусственным» предсказанием не существует. Различие — в субстрате и данных, не в операции.

**Следствие 1.2.** Ни на одном уровне система не оптимизирует «истину». Она оптимизирует *сбываемость*. Истина — это предельный случай максимальной сбываемости на бесконечном горизонте.

---

## §2. Свободная энергия как единая мера

Принцип минимизации свободной энергии (Friston, 2010) — математически точная формулировка Аксиомы 1.

**Определение 2.1.** *Свободная энергия* системы с внутренней моделью $Q$ и наблюдениями $\mathcal{E}$:

$$
F = \underbrace{D_{\text{KL}}\!\left[Q(w) \| P(w \mid \mathcal{E})\right]}_{\text{ошибка модели}} - \underbrace{\log P(\mathcal{E})}_{\text{логарифм Evidence}}
$$

$F$ является **верхней границей удивления**: $F \geq -\log P(\mathcal{E})$.

Минимизация $F$ эквивалентна трём операциям:

$$
\min F \iff \begin{cases}
\text{learning:} & \text{улучшить } Q \text{ так, чтобы } Q \approx P(w \mid \mathcal{E}) \\
\text{action:} & \text{изменить } \mathcal{E} \text{ так, чтобы } P(\mathcal{E}) \text{ стал больше} \\
\text{attention:} & \text{выбрать } \mathcal{E} \text{ с максимальной информационной ценностью}
\end{cases}
$$

Это единая математическая формула **всего когнитивного процесса**: восприятия, действия, внимания.

**Теорема 2.1.** Для системы, реализующей предиктивное кодирование, минимизация $F$ эквивалентна максимизации байесовского evidence:

$$
\min_Q F(Q) = \max_Q \underbrace{\mathbb{E}_Q[\log P(\mathcal{E} \mid w)]}_{\text{точность}} - \underbrace{D_{\text{KL}}[Q(w) \| P(w)]}_{\text{сложность}}
$$

Баланс точности и сложности — это формализация принципа бритвы Оккама.

---

## §3. Предиктивный выигрыш и шкала доверия

**Определение 3.1.** *Предиктивный выигрыш* модели $M$ относительно базовой $M_0$:

$$
\text{PG}(M) = \mathbb{E}\left[\log \frac{M(w_{t+1} \mid w_{\leq t})}{M_0(w_{t+1} \mid w_{\leq t})}\right] = D_{\text{KL}}(M_0 \| M) \geq 0
$$

**Определение 3.2.** *Накопленный предиктивный выигрыш* за $T$ шагов:

$$
\text{NPG}(M, T) = \sum_{t=1}^{T} \log \frac{M(w_t \mid w_{<t})}{M_0(w_t \mid w_{<t})}
$$

Это количество бит, сэкономленных моделью относительно случайного угадывания.

**Теорема 3.1 (Истина как предел).** Абсолютная истина — не данность, а предел:

$$
M^* = \lim_{T \to \infty} \arg\max_M \frac{1}{T} \text{NPG}(M, T)
$$

**Операциональная шкала UAF.** Отображение $\text{NPG} \to [-3, +3]$:

| Уровень | Операциональное определение | Формально |
|---|---|---|
| **+3** | Предсказывает то, что ничего другое не может | $\text{PG}(M) \gg \text{PG}(M_{\text{best}})$ |
| **+2** | Предсказывает лучше любой доступной модели | $\text{PG}(M) > \text{PG}(M_k) \; \forall k$ |
| **+1** | Предсказывает лучше базовой интуиции | $\text{PG}(M) > 0$ |
| **0** | Неотличимо от случайности | $\text{PG}(M) \approx 0$ |
| **−1** | Предсказывает хуже интуиции | $\text{PG}(M) < 0$ (дистрибуционный сдвиг) |
| **−2** | Стабильное анти-предсказание | $\text{NPG}(M, T) \to -\infty$ |
| **−3** | Оптимизирован на несбываемость | $\arg\max_M (-\text{PG}(M))$ |

---

## §4. Байесовское обновление и UAF-весовая схема

**Стандартное обновление (Байес):**

$$
P(M \mid \mathcal{E}) = \frac{P(\mathcal{E} \mid M) \cdot P(M)}{P(\mathcal{E})}
$$

В log-odds представлении — аддитивно по свидетельствам:

$$
L_n = L_0 + \sum_{i=1}^{n} \underbrace{\log \frac{P(e_i \mid M)}{P(e_i \mid \neg M)}}_{\lambda_i = \text{вес свидетельства}}
$$

**Расширенное UAF-обновление.** Чистый байесовский вес $\lambda_i$ корректируется качественными множителями:

$$
\lambda_i^{\text{UAF}} = \lambda_i \cdot R_i \cdot I_i \cdot S_i \cdot F_i \cdot (1 + G_i)
$$

где:

| Множитель | Обозначение | Смысл | Диапазон |
|---|---|---|---|
| Надёжность источника | $R_i$ | Исторический $\frac{\text{NPG}_{\text{source}}}{\text{NPG}_{\text{max}}}$ | $[0, 1]$ |
| Независимость | $I_i$ | $1 - \max_j \text{correlation}(e_i, e_j)$ | $[0, 1]$ |
| Охват | $S_i$ | Доля пространства гипотез, покрытая $e_i$ | $[0, 1]$ |
| Свежесть | $F_i$ | $e^{-\lambda \Delta t}$, где $\Delta t$ — возраст свидетельства | $(0, 1]$ |
| Предиктивный выигрыш | $G_i$ | $\text{PG}(M \mid e_i) - \text{PG}(M)$ | $[0, \infty)$ |

**Итоговое обновление:**

$$
P_{\text{UAF}}(M \mid \mathcal{E}) = \sigma\!\left(L_0 + \sum_{i=1}^{n} \lambda_i^{\text{UAF}}\right), \quad \sigma(x) = \frac{1}{1+e^{-x}}
$$

---

## §5. Диалектика как алгоритм

Гегелевская триада — это не поэтическая метафора. Это точное описание итеративной оптимизации под ограничение сохранения предыдущей модели.

**Определение 5.1 (Тезис).** Начальная параметрическая модель:

$$
M_0: \quad P_0(y \mid x; \theta_0)
$$

**Определение 5.2 (Антитезис).** Резидуальное множество — случаи, где $M_0$ терпит неудачу:

$$
\mathcal{R}(M_0, \theta) = \left\{(x_i, y_i) : -\log P_0(y_i \mid x_i) > \tau\right\}
$$

**Определение 5.3 (Синтез — Aufhebung).** Новая модель, объясняющая и тезис, и антитезис:

$$
M_1 = \arg\min_{M} \left[ \lambda \cdot D_{\text{KL}}(M \| M_0) + \sum_{(x,y) \in \mathcal{R}} -\log M(y \mid x) \right]
$$

$\lambda > 0$ — коэффициент консерватизма: сколько «уважения» к старой модели.

**Теорема 5.1 (Сходимость диалектики).** При $\lambda \to 0$ и $|\mathcal{R}| \to 0$ итерация сходится:

$$
M_0 \xrightarrow{\mathcal{R}_0} M_1 \xrightarrow{\mathcal{R}_1} M_2 \xrightarrow{\mathcal{R}_2} \cdots \longrightarrow M^*
$$

$$
M^* = F(M^*), \quad \text{где } F \text{ — оператор обновления}
$$

$M^*$ — фиксированная точка. Это формальный аналог гегелевской Абсолютной идеи.

**Становление (Werden):**

$$
\text{Werden}(t) = \left\| \frac{d}{dt} P_t(y \mid x) \right\|
$$

- $\text{Werden}(t) \to \infty$: знание нестабильно, хаос
- $\text{Werden}(t) \to 0$: знание кристаллизуется
- $\text{Werden}(t) = \text{const}$: медленное непрерывное обучение

---

## §6. Ноосфера как динамическая система

**Определение 6.1.** *Ноосфера* $\mathcal{N}$ — кортеж:

$$
\mathcal{N} = \left(\mathcal{A},\; \mathcal{M},\; \mathbf{T},\; \mathbf{I}\right)
$$

- $\mathcal{A}$ — множество агентов (нейроны, люди, институты, LLM, культуры)
- $\mathcal{M} = \{M_i\}_{i \in \mathcal{A}}$ — совокупность моделей мира
- $\mathbf{T} \in [0,1]^{|\mathcal{A}| \times |\mathcal{A}|}$ — матрица доверия
- $\mathbf{I}: \mathcal{A} \times \mathcal{A} \to \mathbb{R}^+$ — матрица интенсивности коммуникации

**Динамика агентов:**

$$
\frac{dM_i}{dt} = \underbrace{\sum_{j \in \mathcal{A}} I_{ij} \cdot T_{ij} \cdot (M_j - M_i)}_{\text{социальное обучение}} + \underbrace{\eta_i(t)}_{\text{индивидуальный опыт}}
$$

**Динамика доверия:**

$$
T_{ij}(t+1) = (1 - \beta)\, T_{ij}(t) + \beta \cdot \mathbb{1}\!\left[\hat{y}_j(t) = y_{\text{actual}}(t)\right]
$$

**Теорема 6.1 (Консенсус).** Если граф $\mathcal{G} = (\mathcal{A}, \mathbf{I})$ связен и $T_{ij} > 0$ для всех пар, то:

$$
\lim_{t \to \infty} M_i(t) = M_{\text{consensus}} = \frac{\sum_i w_i M_i(0)}{\sum_i w_i}
$$

Консенсус определяется структурой доверия, а не истиной. Чтобы $M_{\text{consensus}} \approx M^*$, необходимо, чтобы $T_{ij}$ обновлялось строго по предсказательной сбываемости.

**Теорема 6.2 (Авторитет).** При бесконечном горизонте и честном обновлении доверия:

$$
\lim_{t \to \infty} T_{ij}(t) = \frac{\text{NPG}_j}{\max_k \text{NPG}_k}
$$

Авторитет — это не социальный конструкт. Это предельная доля предсказательного выигрыша.

**LLM как элемент ноосферы:**

$$
M_{\text{LLM}}(x_{t+1} \mid x_{\leq t}) \approx \frac{1}{|\mathcal{D}|} \sum_{d \in \mathcal{D}} P_d(x_{t+1} \mid x_{\leq t})
$$

LLM — взвешенный консенсус всех агентов, чьи тексты вошли в корпус $\mathcal{D}$. Не модель истины. Модель коллективного предсказания.

---

## §7. Знание как аттрактор

**Определение 7.1.** Функция Ляпунова для системы познания:

$$
V(P_t) = D_{\text{KL}}\!\left(P_t \| P_{\text{true}}\right) \geq 0
$$

**Теорема 7.1 (Монотонность).** При правильно выбранном $\alpha_t$ (условия Роббинса-Монро: $\sum_t \alpha_t = \infty$, $\sum_t \alpha_t^2 < \infty$):

$$
V(P_{t+1}) - V(P_t) \leq 0
$$

Каждое обновление не увеличивает расстояние до истины.

**Теорема 7.2 (Предельная истина).** При $P_{\text{data}, t} \to P_{\text{true}}$:

$$
P_t \xrightarrow{\text{a.s.}} P_{\text{true}}
$$

**Скорость сходимости:**

$$
\rho = \limsup_{t \to \infty} \frac{V(P_{t+1})}{V(P_t)}
$$

- $\rho < 1$: экспоненциальная сходимость (нормальная наука)
- $\rho = 1$: субэкспоненциальная сходимость (зрелая парадигма)
- $\rho > 1$: дивергенция (кризис парадигмы по Куну)

**Горизонт знания** — минимальное время достижения точности $K$:

$$
\tau_K = \min\left\{t : D_{\text{KL}}(P_t \| P_{\text{true}}) < K\right\}
$$

Абсолютная истина требует $\tau_0 = \infty$. Практическое знание — $\tau_K$ при конечном $K$.

---

## §8. Кризис парадигмы

**Определение 8.1.** *Парадигма* — параметрическое семейство с ограниченным пространством гипотез:

$$
\mathcal{P} = \{M_\theta : \theta \in \Theta\}
$$

**Нормальная наука** — оптимизация внутри $\Theta$:

$$
\theta_t^* = \arg\min_{\theta \in \Theta} \sum_{s \leq t} -\log M_\theta(w_s \mid w_{<s})
$$

**Аномалия** — накопление необъяснимых наблюдений:

$$
\mathcal{R}(t) = \left\{w_s : -\log M_{\theta_t^*}(w_s \mid w_{<s}) > \tau_{\text{anom}}\right\}
$$

**Условие революции** (переход $\mathcal{P} \to \mathcal{P}'$):

$$
\sum_{w \in \mathcal{R}(t)} \log \frac{M_{\theta'}(w)}{M_{\theta^*}(w)} > \Delta_{\text{rev}}, \quad \Theta' \supsetneq \Theta
$$

Новая парадигма оправдана тогда и только тогда, когда она существенно лучше объясняет аномалии.

**Несоизмеримость (Кун).** $D_{\text{KL}}(P_{\mathcal{P}} \| P_{\mathcal{P}'}) \neq D_{\text{KL}}(P_{\mathcal{P}'} \| P_{\mathcal{P}})$ — дивергенция несимметрична. Разрешение через симметричный критерий:

$$
\Delta_{\text{PG}} = \text{PG}(\mathcal{P}') - \text{PG}(\mathcal{P})
$$

---

## §9. Формальная этика предсказания

**Определение 9.1.** *Ответственность агента $j$* за решение агента $i$:

$$
\text{Resp}(j \to i) = D_{\text{KL}}\!\left(\hat{P}_j \| P_{\text{true}}\right) \cdot \left|U_i(a_i^*) - U_i(a_i^{\text{opt}})\right|
$$

Ответственность пропорциональна произведению ошибки предсказания и величины потери от неправильного решения.

**Категорический императив (Кант → UAF):**

$$
\forall i, j \in \mathcal{A}: \quad \text{Resp}(i \to j) \leq \epsilon_{\text{acc}}
$$

Поступай так, чтобы твоя ошибка предсказания, распространённая на всех, не причиняла неприемлемого ущерба.

**Утилитаризм предсказания:**

$$
\hat{P}^* = \arg\max_{\hat{P}} \sum_{i,o} P_{\text{true}}(o) \cdot U_i(o) \quad \text{s.t.} \quad D_{\text{KL}}(\hat{P} \| P_{\text{true}}) \leq \delta
$$

Максимизировать общественное благо при ограничении на точность предсказания.

**Честность как доминирующая стратегия:**

$$
V_{\text{honest}} = \sum_{t=1}^{\infty} \delta^t \cdot \text{PG}(t) > V_{\text{manipulate}}
$$

В системе с открытым предсказательным учётом честность — не мораль, а вычислимый оптимум.

---

## §10. Антиномии и их разрешение

**Определение 10.1.** *Антиномия* — пара $(T, \neg T)$ такая, что при ограниченном опыте:

$$
P(T \mid \mathcal{E}_{\text{bounded}}) \approx P(\neg T \mid \mathcal{E}_{\text{bounded}})
$$

Антиномия Канта — не логический тупик. Это **сигнал о недостаточности свидетельств**.

Предиктивное разрешение через Bayes Factor:

$$
\text{BF} = \frac{P(\mathcal{E} \mid M_T)}{P(\mathcal{E} \mid M_{\neg T})}
$$

- $\text{BF} \gg 1$: $T$ предпочтительнее
- $\text{BF} \approx 1$: вопрос неразрешим при текущих данных — и это честный ответ
- Требовать ответа при $\text{BF} \approx 1$ — эпистемическая ошибка

---

## §11. Категорная структура познания

**Категория $\mathbf{Kn}$:**

- **Объекты:** модели $M_i$
- **Морфизмы:** $f: M_i \to M_j$, где $M_j$ получается расширением $M_i$ новыми переменными: $P_j(y \mid x) = P_i(y \mid x, z)$
- **Тождество:** $\text{id}_{M_i}: M_i \to M_i$
- **Композиция:** последовательное расширение моделей

**Функтор познания:**

$$
\mathcal{F}: \mathbf{World} \to \mathbf{Kn}
$$

Каждой ситуации в мире сопоставляется модель знания.

**Научная революция** — естественное преобразование $\eta: \mathcal{F} \Rightarrow \mathcal{G}$ между двумя функторами познания. Она сохраняет структуру (доказуемость, предсказание), меняя конкретные модели.

**Топос познания.** В топосе $\mathcal{T}$ значения истинности — не $\{0, 1\}$, а решётка $\text{Sub}(\Omega)$:

$$
\text{Truth} = \text{Sub}(\Omega), \quad \bot \leq p \leq \top
$$

Истина — спектр, а не бинарный флаг.

---

## §12. Таблица соответствий

| Философ | Понятие | Формула | Операциональный смысл |
|---|---|---|---|
| Кант | Априорные формы | $\Phi^* = \arg\min_\Phi \mathbb{E}[-\log P_\Phi(w_{t+1})]$ | Пространство и время — выученные предсказательные метрики |
| Кант | Категории рассудка | $c_i: \mathcal{E}^n \to \mathcal{J}$ | Структурирующие функции: cause, necessity, possibility |
| Кант | Единство апперцепции | $\mathcal{A}: \mathcal{E}^* \to \mathbb{R}^d$ | Сжатие всей истории в один вектор состояния |
| Кант | Антиномии | $\text{BF} \approx 1$ | Сигнал недостаточности свидетельств |
| Кант | Категорический императив | $\text{Resp}(i \to j) \leq \epsilon$ | Ошибка предсказания × ущерб ≤ допустимого |
| Гегель | Тезис-антитезис-синтез | $M_1 = \arg\min[\lambda D_{KL} + \sum -\log M(y\|x)]$ | Итеративная оптимизация с сохранением prior |
| Гегель | Становление | $\|\frac{d}{dt}P_t\|$ | Скорость изменения модели |
| Гегель | Aufhebung | $L_{k+1} = L_k \cup \{\text{новые переменные}\}$ | Расширение пространства гипотез |
| Гегель | Абсолютная идея | $M^* = F(M^*)$ | Фиксированная точка оператора обновления |
| Гегель | Дух познаёт себя | $I(\mathcal{W}; \mathcal{M}) \to H(\mathcal{W})$ | Взаимная информация мира и модели |
| Кун | Парадигма | $\mathcal{P} = \{M_\theta : \theta \in \Theta\}$ | Ограниченное пространство гипотез |
| Кун | Кризис | $\rho > 1$ | Дивергенция функции Ляпунова |
| Кун | Революция | $\Theta' \supsetneq \Theta$, $\Delta_{\text{PG}} > \Delta_{\text{rev}}$ | Расширение пространства при достаточном выигрыше |
| Кун | Несоизмеримость | $D_{KL}(P\|Q) \neq D_{KL}(Q\|P)$ | Разрешается через симметричный PG |
| Фристон | Свободная энергия | $F = D_{KL}[Q\|P(w\|\mathcal{E})] - \log P(\mathcal{E})$ | Верхняя граница удивления |
| Шеннон | Энтропия | $H(X) = -\sum P(x)\log P(x)$ | Средняя сложность предсказания |
| Байес | Обновление | $P(M\|\mathcal{E}) \propto P(\mathcal{E}\|M)P(M)$ | Рациональное обновление доверия |
| Вернадский | Ноосфера | $\mathcal{N} = (\mathcal{A}, \mathcal{M}, \mathbf{T}, \mathbf{I})$ | Динамическая система предсказывающих агентов |

---

## §13. Заключительные тезисы

**Тезис 1 (Единство).** Все формы познания реализуют одну операцию:

$$
\boxed{\min_\theta \; \mathbb{E}\!\left[-\log P_\theta(\text{future} \mid \text{past})\right]}
$$

**Тезис 2 (Истина).** Истина — предел, а не данность:

$$
M^* = \lim_{T \to \infty} \arg\max_M \frac{1}{T}\,\text{NPG}(M, T)
$$

**Тезис 3 (Доверие).** Доверие — мера, а не отношение:

$$
T_{ij} = \frac{\text{NPG}_j}{\max_k\,\text{NPG}_k}
$$

**Тезис 4 (Ноосфера).** Ноосфера — не метафора:

$$
\frac{dM_i}{dt} = \sum_j I_{ij}\,T_{ij}(M_j - M_i) + \eta_i
$$

**Тезис 5 (Этика).** Этично то, что минимизирует суммарное удивление:

$$
\text{Ethics} = \arg\min_a \sum_i H_{\text{pred},i}(a)
$$

**Тезис 6 (Программирование).** Хорошая программа — машина уменьшения удивления:

$$
\text{GoodProgram} = \arg\min_P\, \mathbb{E}_{\text{user}}[S_{\text{user}} \mid P]
$$

**Тезис 7 (LLM).** LLM — интерфейс к ноосфере, а не имитация разума:

$$
M_{\text{LLM}}(x_{t+1} \mid x_{\leq t}) \approx \frac{1}{|\mathcal{D}|}\sum_{d \in \mathcal{D}} P_d(x_{t+1} \mid x_{\leq t})
$$

---

## Финальная формула

$$
\boxed{
\text{Истина} = \lim_{t \to \infty} \frac{1}{t} \sum_{\tau=1}^{t} \log \frac{M(w_\tau \mid w_{<\tau})}{M_0(w_\tau \mid w_{<\tau})}
}
$$

*Предельный средний предиктивный выигрыш над случайностью.*  
*Всё остальное — следствия.*

---

## Ссылки и источники

- **Friston, K.** (2010). The free-energy principle: a unified brain theory? *Nature Reviews Neuroscience*, 11, 127–138.
- **Shannon, C.E.** (1948). A Mathematical Theory of Communication. *Bell System Technical Journal*, 27, 379–423.
- **Bayes, T.** (1763). An Essay towards solving a Problem in the Doctrine of Chances. *Phil. Trans. Royal Soc.*
- **Kuhn, T.** (1962). *The Structure of Scientific Revolutions*. University of Chicago Press.
- **Vernadsky, V.** (1926). *The Biosphere*. Leningrad.
- **Hegel, G.W.F.** (1807). *Phänomenologie des Geistes*.
- **Kant, I.** (1781). *Kritik der reinen Vernunft*.
- **Robbins, H., Monro, S.** (1951). A stochastic approximation method. *Ann. Math. Stat.*, 22, 400–407.
- **Grothendieck, A.** (1972). *SGA 4: Théorie des Topos*.

---

*Этот документ — часть репозитория [UAF](https://github.com/131ymm-commits).*  
*Каждый тезис открыт для опровержения. Единственный принятый метод опровержения — предсказательный.*
